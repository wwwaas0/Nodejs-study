// 1. 다음 배열의 모든 원소의 값을 10씩 더해주기
const arr = [1, 2, 3];

// 2. 다음 배열의 모든 원소의 값을 자신의 인덱스와 더해주기(map이 아닌 함수)
const arr2 = [10, 20, 30];

// 2. 다음 배열의 모든 원소의 값을 자신의 인덱스와 더해주기(map)
const arr3 = [10, 20, 30];
